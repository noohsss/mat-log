# 맛로그(mat-log) 서비스 기획서 (MVP)

## Context
사용자가 여러 곳에서 알게 된 맛집을 "주제별 / 대상별 / 음식 종류별"로 정리해서 기록하고 다시 찾아볼 수 있는 맛집 메모장 서비스가 필요함. 처음엔 개인용 메모장으로 시작했으나, 논의 과정에서 "내가 등록한 맛집이 다른 로그인 사용자에게도 추천 게시판 형태로 노출되고, 그들이 검색/필터링해서 찾아볼 수 있어야 한다"는 요구사항이 추가되어 최소한의 공유(공개 게시판) 기능까지 MVP 범위에 포함하기로 함. 테이블(스키마) 설계는 Claude가 초안을 제안하고 사용자가 검토·수정하여 확정하는 방식으로 진행함.

## 확정된 요구사항
- **서비스 형태**: 이메일/비밀번호 회원가입·로그인만 지원하는 웹앱 (소셜 로그인 사용 안 함). 로그인한 사용자는 각자 자신의 맛집을 기록하고, 등록한 맛집은 모두 자동으로 공개되어 다른 로그인 사용자도 볼 수 있는 "추천 맛집 게시판"에 노출됨
- **공개 게시판 접근권**: 게시판 열람/검색/필터링도 로그인 필요 (비로그인 사용자는 접근 불가)
- **공개 범위**: 등록한 맛집은 별도 공개 설정 없이 전부 자동 공개 (비공개 옵션은 MVP 범위 아님)
- **분류 기준**: 주제 / 대상 / 음식 종류 + 지역·위치 + 평점·가격대
- **사진 첨부**: MVP 제외 (텍스트 정보 중심으로 우선 구현)
- **기술 스택**: Next.js + Supabase (Postgres DB + Auth의 이메일/비밀번호 기능 사용, Storage는 MVP 미사용)

## MVP 기능 범위

### 1. 인증
- 이메일/비밀번호 회원가입(가입 시 이메일 인증 필요) · 로그인 · 로그아웃
- 로그인한 사용자만 서비스 전체(내 맛집, 공개 게시판 모두)에 접근 가능

### 2. 맛집 기록 CRUD
- 맛집 등록: 이름, 지역/위치(텍스트 입력 또는 간단한 주소), 음식 종류, 평점, 가격대, 방문 여부(방문함/방문 안함, 기본값 방문함), 한줄평(자유 텍스트)
- 주제 태그(예: 데이트, 회식, 혼밥)와 대상 태그(예: 친구, 가족, 연인)를 지정
  - 태그는 사용자가 자유롭게 추가할 수 있는 방식(자유 입력형) 또는 미리 정의된 목록 중 선택하는 방식 중 사용자가 테이블 설계 시 결정
- 본인이 등록한 맛집만 수정/삭제 가능
- 내가 등록한 맛집 목록 조회 (마이페이지/내 목록 뷰)

### 3. 공개 추천 게시판
- 다른 사용자가 등록한 맛집만 모여 있는 공개 게시판 (본인이 등록한 맛집은 게시판에서 제외되고 "내 맛집"에서만 보임)
- 등록자 본인이 아니므로 열람 + "저장" 버튼만 노출 (수정/삭제 불가)
- "저장" 클릭 시 해당 맛집이 방문 안함 상태로 복사되어 내 맛집 목록에 추가됨 (원본의 평점·한줄평은 가져오지 않음 — 직접 방문 후 작성)
- "내 맛집"에서 방문 안함 항목은 별점을 숨기고 "방문 후 별점과 한줄평을 작성해주세요." 안내문을 대신 표시
- "저장" 후에도 게시판 화면에 그대로 머무름 (내 맛집으로 이동하지 않음)
- 이미 저장한 맛집(원본)은 게시판에서도 제외됨 (중복 저장 노출 방지)
- 게시판에서도 아래 분류/필터링·검색이 동일하게 적용됨

### 4. 분류/필터링
- 주제별, 대상별, 음식 종류별, 지역별 필터링
- 평점/가격대 기준 정렬 또는 필터링
- 여러 조건 동시 적용 가능 (예: "친구" + "한식" + "저렴")
- "내 목록"과 "공개 게시판" 양쪽 뷰에 동일하게 적용

### 5. 검색
- 맛집 이름 또는 한줄평 텍스트로 간단한 키워드 검색
- "내 목록"과 "공개 게시판" 양쪽 뷰에서 사용 가능

### 6. AI 자연어 추천 검색 (Gemini API)
- 추천 게시판 우측 하단 플로팅 버튼을 누르면 열리는 채팅 패널로 제공 (기존 필터/검색과 독립적으로 동작). 질문/추천 결과가 대화 형태로 쌓여 여러 번 이어서 물어볼 수 있음
- "여자친구랑 갈만한 조용한 한식집 추천해줘" 같은 자연어 요청을 입력하면, 게시판 후보(본인 글 제외, 최신순 최대 50개) 중에서 Gemini(`gemini-2.5-flash`)가 가장 적합한 곳을 최대 10개 골라 한 줄 추천 이유와 함께 보여줌
- Gemini는 응답 스키마를 구조화된 JSON(`{ recommendations: [{ id, reason }] }`)으로 강제받고, 반환된 id는 후보 목록과 대조해 검증 후에만 사용 (환각 방지)
- `GEMINI_API_KEY` 미설정/호출 실패 시 에러 문구만 표시되고 기존 게시판 기능에는 영향 없음

## 테이블 설계 (확정)
Claude가 제안하고 사용자가 검토하여 확정한 스키마.

### users
사용자 프로필 (게시판에 등록자 이름을 표시하기 위해 `auth.users`와 별도로 유지, 가입 시 트리거로 자동 생성)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK, = auth.users.id) | |
| email | varchar(255) | auth.users.email 복사본 (게시판 등에서 조회용) |
| nickname | varchar(50) | 게시판에 노출되는 닉네임 |
| created_at | timestamptz | |

### restaurants
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| user_id | uuid (FK → users.id) | 작성자 |
| name | varchar(100) | 맛집 이름 |
| region | varchar(255) | 지역/위치 (카카오맵 주소 검색으로 채워짐, 직접 입력도 허용) |
| lat | double precision (nullable) | 주소 검색 결과의 위도 — 등록/수정 폼·상세 페이지 지도 표시용 |
| lng | double precision (nullable) | 주소 검색 결과의 경도 |
| food_type | enum (food_type) | 고정 목록, 아래 참고 |
| price_range | enum (price_range) | 고정 목록, 아래 참고 |
| rating | numeric(2,1) | 0.0~5.0 |
| visited | boolean | 방문 여부 (기본값 true) — 게시판에서 "저장"한 맛집은 false(방문 안함)로 생성 |
| source_restaurant_id | uuid (FK → restaurants.id, nullable) | "저장"으로 복사됐다면 원본 맛집 id — 원본은 게시판에서 제외됨 |
| memo | text | 자유 한줄평 (길이 제한 없음) |
| photo_url | text (nullable) | 대표사진 1장 (Supabase Storage `restaurant-photos` public 버킷의 공개 URL) |
| created_at | timestamptz | |
| updated_at | timestamptz | |

- `food_type` enum(확정): 한식, 중식, 일식, 양식, 카페·디저트, 술집·바, 기타
- `price_range` enum(확정): 저렴(₩), 보통(₩₩), 비쌈(₩₩₩)

### tags / restaurant_tags (정규화된 태그, 채택안 A)
| 테이블 | 컬럼 | 설명 |
|---|---|---|
| tags | id, name (varchar(50)), type ('topic'\|'target') | 태그 마스터, unique(name, type) |
| restaurant_tags | restaurant_id (FK), tag_id (FK) | 다대다 연결, PK(restaurant_id, tag_id) |

### 권한 (RLS) 방향
- `restaurants`, `restaurant_tags`: 로그인 사용자는 전체 SELECT 가능. INSERT는 `user_id = auth.uid()`. UPDATE/DELETE는 `user_id = auth.uid()`인 행만 가능
- `tags`: 로그인 사용자 전체 SELECT, INSERT는 로그인 사용자 누구나(새 태그 자유 추가)
- `users`: 전체 SELECT(등록자 이름 표시용), 본인 행만 UPDATE, 가입 트리거로만 INSERT

## 화면 구성 (MVP)
사용자가 와이어프레임을 전달함 (레이아웃 확정, 테마/색상은 Claude가 담당). 공통 레이아웃: 상단에 "맛로그" 로고 + 로그인 사용자 이름/아바타/로그아웃, 그 아래 탭 바(추천 게시판 / 내 맛집 / 맛집 등록).

- **로그인/회원가입 화면**: 로고, "발견한 맛집을 기록하고 다른 사용자의 추천도 찾아보세요" 문구, 이메일·비밀번호 입력 폼 + 로그인/회원가입 전환
- **추천 게시판 화면**: 전체 사용자의 맛집 목록. 검색창(이름/한줄평), 필터(주제/대상/음식 종류/지역 드롭다운) + 정렬(최신순 등), 총 개수와 적용된 필터 개수 표시. 카드형 리스트: 맛집명, 평점, 지역·음식종류·가격대, 태그(#데이트 #친구 등), 한줄평 미리보기, "상세 보기" 링크
- **내 맛집 화면**: 추천 게시판과 동일한 검색/필터/카드 UI이지만 내가 등록한 것만 표시, 각 카드에 "수정" 링크 추가
- **상세 화면**: 좌측에 맛집명/기본정보(위치, 음식종류, 가격대, 평점)와 한줄평 전문, 우측에 등록자 정보(이름, 등록일)와 "내가 등록한 맛집" 안내 + 수정/삭제 버튼(작성자 본인에게만 노출)
- **등록/수정 화면**: "기록한 정보는 추천 게시판에 자동으로 공개됩니다" 안내문. 기본 정보(이름, 지역·위치, 음식 종류 선택, 평점 선택, 가격대 선택), 분류 태그(주제 태그/대상 태그 입력), 한줄평(자유 텍스트), 하단 취소/등록 버튼

## 화면 구성 - 디자인 담당
- **레이아웃/구조**: 확정 (사용자 와이어프레임 기준)
- **테마/색상 등 비주얼 디자인**: Claude가 담당 (컬러 팔레트, 타이포그래피, 컴포넌트 스타일 등)
- **기본 테마**: 라이트 모드 (와이어프레임은 다크 톤으로 그려졌지만 참고용 레이아웃일 뿐, 실제 기본 테마는 라이트 모드로 결정). 다크 모드는 MVP 이후 백로그로 이동

## 이번 기획에서 제외된 기능 (MVP 이후 백로그)
- 소셜 로그인(Google, Kakao, GitHub 등) — 카카오 로그인 전환 논의했으나 기존 이메일/비밀번호 계정 유지를 위해 보류
- 맛집별 공개/비공개 선택 (현재는 등록 시 전부 자동 공개)
- 비로그인 사용자의 게시판 열람 (현재는 로그인 필수)
- 사진 여러 장(갤러리) 첨부 — 대표사진 1장은 MVP 이후 추가로 지원함
- 좋아요, 댓글, 팔로우 등 소셜 상호작용 기능
- 재방문 알림, 방문 이력(날짜별 기록) — 방문 여부(방문함/방문 안함) 자체는 MVP에 포함됨
- 외부 맛집 정보 연동 (네이버지도/카카오맵 API로 자동 정보 채우기)
- 추천 알고리즘 (예: 개인화 추천, 인기순 랭킹)
- 다크모드 등 UI 커스터마이징
- 모바일 앱(네이티브) 버전
- 데이터 export/import, 백업

## 검증 방법
- `npm run dev`로 로컬 개발 서버 구동 후 브라우저에서 이메일 회원가입(인증 메일 확인) → 로그인 → 맛집 등록 → 공개 게시판에서 다른 계정으로 로그인해 해당 맛집이 조회되는지 확인 → 필터링/검색 → 본인 계정으로 수정/삭제 전체 플로우 수동 테스트
- Supabase RLS 정책이 (1) 전체 로그인 사용자에게 SELECT를 허용하면서 (2) 작성자 본인만 UPDATE/DELETE 가능한지 두 개의 테스트 계정으로 교차 확인

## 진행 로그
- [x] Next.js + Supabase 프로젝트 세팅, 스키마/RLS 마이그레이션 적용
- [x] (임시) seed 계정으로 게시판 데이터 연결 테스트 — 실제 로그인 붙으면 제거 예정
- [x] 이메일/비밀번호 인증
- [x] 맛집 등록/수정/삭제
- [x] 공개 추천 게시판 — 추천 게시판/내 맛집 탭 분리
- [x] 필터링/검색 — 주제·대상·음식종류·지역 필터, 이름/한줄평 검색, 정렬(최신순·평점순) — 가격대는 3단계뿐이라 정렬 제외
- [x] AI 자연어 추천 검색 — Gemini API 연동, 게시판 후보 중에서만 추천하도록 검증
- [x] 목록 페이지네이션 — 추천 게시판/내 맛집 12개 단위 페이지네이션, 필터/검색/정렬 변경 시 1페이지로 리셋
- [x] AI 추천 검색 요청 제한 — ai_recommendation_limits 테이블로 사용자당 분당 5회 제한
- [x] 카카오맵 주소 검색 — Kakao Local REST API로 지역·위치 키워드 검색 자동완성, 결과 좌표를 저장해 등록/수정 폼과 상세 페이지에 지도로 표시
- [x] 게시판/내 맛집 카드 지역 표기 개선 — 뱃지 대신 풀 주소 텍스트로 노출
- [x] 맛집 상세 페이지 — `/restaurants/[id]`, 카드 클릭 시 이동, 소유자만 수정/삭제 노출
- [x] 맛집 대표사진 업로드 — Supabase Storage 연동, 카드·상세 페이지에 노출, "저장" 시 원본 사진 이관 (사진 없을 때도 플레이스홀더 노출)
- [x] AI 추천 검색 UX 개편 — 우측 하단 플로팅 채팅 위젯으로 전환, 대화 형태로 여러 질문 누적
